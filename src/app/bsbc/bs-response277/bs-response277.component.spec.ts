import { ComponentFixture, TestBed } from '@angular/core/testing';

import { BsResponse277Component } from './bs-response277.component';

describe('BsResponse277Component', () => {
  let component: BsResponse277Component;
  let fixture: ComponentFixture<BsResponse277Component>;

  beforeEach(() => {
    TestBed.configureTestingModule({
      declarations: [BsResponse277Component]
    });
    fixture = TestBed.createComponent(BsResponse277Component);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
