import { ComponentFixture, TestBed } from '@angular/core/testing';

import { BsRequest276Component } from './bs-request276.component';

describe('BsRequest276Component', () => {
  let component: BsRequest276Component;
  let fixture: ComponentFixture<BsRequest276Component>;

  beforeEach(() => {
    TestBed.configureTestingModule({
      declarations: [BsRequest276Component]
    });
    fixture = TestBed.createComponent(BsRequest276Component);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
